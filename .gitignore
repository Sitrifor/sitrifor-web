# Secrets - never commit
.env
.env.*
!.env.example
!**/.env.example
seo/credentials/*
!seo/credentials/.gitkeep
!seo/credentials/README.md
*.pem
*.p12
*.key
id_rsa
id_ed25519
*_token.txt
*-oauth*.json
.analytics-token
!.env.example

# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc
.Python
.npm/
.cache/

# Build / generated / local reports
seo/reports/
*.log
.DS_Store
Thumbs.db

# Local backups and junk
public-dark-backup-*/
**/*.bak
**/*.bak-*
**/*backup*/
masters.html.broken-*

# Editor / OS
.idea/
.vscode/
*.swp
*~

# GitHub / Cursor local only (keep rules)
.cursor/*.log

# Runtime SQLite and writer state (keep local, do not publish)
backend/data/*.db
backend/data/*.db-*
backend/data/*-writer-state.json

# RusLawOD corpus (multi-GB) and derived DB
backend/data/ruslawod/download/
backend/data/ruslawod/extract/
backend/data/ruslawod/*.db
backend/data/ruslawod/*.db-*

# Lawyer frontend deps (built assets live in public/lawyer)
lawyer-app/node_modules/
lawyer-app/dist/
lawyer-app/*.tsbuildinfo
.venv-scrape/

# Scraped / local SEO datasets
seo/data/
